# Choose black or white text for contrast against a background color

Uses the WCAG relative luminance formula.

## Usage

``` r
.contrast_color(hex_color)
```

## Arguments

- hex_color:

  A single hex color string (e.g. `"#A8C5DA"`).

## Value

`"#1C1B1A"` (dark) or `"#FFFFFF"` (light).
