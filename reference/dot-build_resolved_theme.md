# Build a complete resolved theme for one track

Combines the per-axis specs for all four sides, the chrome spec, and a
few convenience scalars. Stored on the track for the draw pipeline.

## Usage

``` r
.build_resolved_theme(flat_theme)
```

## Arguments

- flat_theme:

  Merged flat theme (plot + track).

## Value

Nested list.
