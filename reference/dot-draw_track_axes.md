# Draw all active axes for one track

Draw all active axes for one track

## Usage

``` r
.draw_track_axes(track, panels)
```

## Arguments

- track:

  A `SeqTrackR6`.

- panels:

  Panel list (from `layoutGrid()`).

## Value

Invisibly `NULL`.
