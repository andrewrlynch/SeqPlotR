# Detect the signal column in a GRanges

Detect the signal column in a GRanges

## Usage

``` r
.detect_signal_col(gr, hint = NULL)
```

## Arguments

- gr:

  A `GRanges` with a numeric signal column.

- hint:

  Optional explicit column name.

## Value

The detected column name.
