# Precompute the resolved track chrome spec

Precompute the resolved track chrome spec

## Usage

``` r
.build_chrome_spec(flat_theme)
```

## Arguments

- flat_theme:

  Flattened theme.

## Value

Nested list with `background`, `border`, and `window` sections.
