% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_ideogram.R
\name{seq_ideogram}
\alias{seq_ideogram}
\title{Chromosome ideogram from cytogenetic bands}
\usage{
seq_ideogram(
  data = NULL,
  mapping = NULL,
  aesthetics = aes(),
  scope = "window",
  style = "block",
  highlight_range = NULL,
  ...
)
}
\arguments{
\item{data}{Optional \code{GRanges} of cytobands. Falls back to the parent
track's data. Must carry a \code{gieStain} mcol unless a \code{stain} mapping
is supplied.}

\item{mapping}{Optional \code{\link[=map]{map()}}. Recognised: \code{stain}.}

\item{aesthetics}{Optional \code{\link[=aes]{aes()}}: \code{color} (band border color,
default \code{"black"}), \code{linewidth} (band border width, default
\code{0.1}), \code{outline} (nested \code{aes()} controlling the chromosome's
outer perimeter outline: sub-keys \code{col}, \code{lwd}, \code{visible}),
\code{highlight} (nested \code{aes()} controlling the \code{scope = "full"}
highlight rectangle: sub-keys \code{fill}, \code{col}, \code{lwd}, \code{alpha}),
\code{telomere.radius} (numeric; corner radius as a fraction of band
height for \code{style = "rounded"}; \code{1.0} = full half-circle cap).}

\item{scope}{Character. One of \code{"window"} (default — only bands
overlapping the track windows are drawn) or \code{"full"} (the whole
chromosome is drawn rescaled to fill the panel; the current window
region is overlaid as a translucent highlight rectangle).}

\item{style}{Character. One of \code{"block"} (default — rectangular
bands) or \code{"rounded"} (rounded telomere caps on the leftmost and
rightmost bands).}

\item{highlight_range}{Optional \code{GRanges}. Only honoured when
\code{scope = "full"}. When set, the highlight rectangle marks this
range instead of the parent track's \code{windows}. Lets you set the
track's \code{windows} to span the full chromosome (so the x-axis
reads chromosome coordinates) while still highlighting a
sub-range.}

\item{...}{Reserved.}
}
\value{
A \code{SeqIdeogramR6} instance.
}
\description{
Draws a chromosome ideogram: each cytoband becomes a filled rectangle
shaded by its Giemsa stain (\code{gpos25} darkens through \code{gpos100}; \code{gneg}
is white; \code{stalk} and \code{gvar} carry their conventional colors). Paired
\code{acen} bands within a window render as two inward-pointing red
triangles, marking the centromere.
}
\details{
The simplest call supplies a \code{GRanges} of cytobands — use
\code{\link[=load_cytobands]{load_cytobands()}} to load the bundled hg38 table:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cb <- load_cytobands()
seq_plot() \%|\%
  seq_track(track_id = "Ideo",
            windows = default_genome_windows()) \%+\%
  seq_ideogram(data = cb)
}\if{html}{\out{</div>}}

Map a non-standard stain column with \code{map(stain = my_col)}.
}
