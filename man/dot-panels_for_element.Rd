% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis.R
\name{.panels_for_element}
\alias{.panels_for_element}
\title{Remap a per-window panel list so xscale/yscale reflect the element's
primary/secondary axis selection}
\usage{
.panels_for_element(layout_track, resolved)
}
\arguments{
\item{layout_track}{List of per-window panel metadata.}

\item{resolved}{Element \code{resolved} list (contains \code{axis_x}/\code{axis_y}).}
}
\value{
A layout_track with \code{xscale} / \code{yscale} rewritten if needed.
}
\description{
When an element sets \code{map(axis.x = 2)} or \code{map(axis.y = 2)}, its
coordinate transforms must use \code{xscale2} / \code{yscale2} instead of the
primary scales. Every primitive's \code{prep()} reads \code{panel$xscale} /
\code{panel$yscale} directly, so this helper builds a per-element shallow
copy of the panel list with those fields swapped.
}
\keyword{internal}
