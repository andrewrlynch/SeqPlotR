% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/element_base.R
\name{.normalize_axis_selector}
\alias{.normalize_axis_selector}
\title{Normalise \code{map(axis.x = ...)} / \code{map(axis.y = ...)} to a scalar integer}
\usage{
.normalize_axis_selector(v, which = "x")
}
\arguments{
\item{v}{Raw resolved value (may be \code{NULL}, vector, or scalar).}

\item{which}{Either \code{"x"} or \code{"y"} — used for the error message.}
}
\value{
Integer scalar in \verb{\{1L, 2L\}}. Defaults to \code{1L} when \code{v} is NULL.
}
\description{
Axis selectors are per-element — they route the element to the primary
(1) or secondary (2) axis. \code{.resolve_mapping()} broadcasts scalars to
the data length; collapse back to a single integer and validate.
}
\keyword{internal}
