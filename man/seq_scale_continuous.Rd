% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{seq_scale_continuous}
\alias{seq_scale_continuous}
\title{Continuous position scale}
\usage{
seq_scale_continuous(
  limits = NULL,
  n_breaks = 5,
  breaks = NULL,
  minor_breaks = NULL,
  expand = c(0.025, 0),
  cap = c("capped", "full", "exact", "ticks"),
  labels = NULL,
  oob = c("exclude", "perimeter"),
  pretty = NULL
)
}
\arguments{
\item{limits}{Optional numeric vector of length 2 clamping the axis range.
If \code{NULL}, the range is auto-computed from element data.}

\item{n_breaks}{Target number of pretty breaks (default 5). Ignored when
\code{breaks} is supplied.}

\item{breaks}{Optional numeric vector of explicit break positions.}

\item{minor_breaks}{Optional scalar (number of sub-divisions between
major breaks) or numeric vector of explicit minor-break positions.}

\item{expand}{Length-2 \code{c(mul, add)} specifying padding around the
data range. \code{mul} is multiplied by \code{diff(data_range)}; \code{add} is
added in data units. Default \code{c(0.025, 0)} — a 2.5\% breath.}

\item{cap}{One of \code{"capped"} (default — axis line spans the break
range, so tick labels don't look stranded), \code{"full"} (spans the
expanded plot range), \code{"exact"} (spans the unexpanded data range),
or \code{"ticks"} (suppress the axis line entirely).}

\item{labels}{Optional character vector of tick labels (same length
as \code{breaks}). When \code{NULL}, breaks are formatted automatically.}
}
\value{
A \code{SeqScaleContinuous_Pos} / \code{SeqPositionScale} object.
}
\description{
Creates a numeric position scale for axes displaying continuous data.
}
