% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_zoom.R
\name{seq_zoom}
\alias{seq_zoom}
\title{Zoom / highlight polygon between two tracks}
\usage{
seq_zoom(
  data = NULL,
  mapping = NULL,
  t0 = NULL,
  t1 = NULL,
  aesthetics = aes(),
  ...
)
}
\arguments{
\item{data}{Optional \code{GRanges} or \code{data.frame}.}

\item{mapping}{Optional \code{\link[=map]{map()}}.}

\item{t0, t1}{Track identifiers (required).}

\item{aesthetics}{Optional \code{\link[=aes]{aes()}}: \code{fill} (default \code{"grey50"}),
\code{alpha} (default \code{0.15}), \code{color} (border; default \code{NA}),
\code{linewidth} (default \code{0.5}), \code{stemOffset} (default \code{0.01}).}

\item{...}{Reserved.}
}
\value{
A \code{SeqZoomR6} instance.
}
\description{
Projects a genomic region onto two tracks and connects the two projections
with a filled quadrilateral. Typically used to link an overview track to
a zoomed detail track (e.g. ideogram → locus). Always used as a plot-level
link — \code{t0} and \code{t1} must be specified explicitly.
}
\details{
Map vocabulary:
\describe{
\item{\code{x0}, \code{x0_end}}{required — genomic edges of the region in \code{t0}.}
\item{\code{x1}, \code{x1_end}}{optional — genomic edges of the region in \code{t1};
default to \code{x0}, \code{x0_end} (same region projected onto both tracks).}
\item{\code{chrom0}, \code{chrom1}}{optional for GRanges (default \code{seqnames(data)});
required for \code{data.frame}.}
}

Attachment edges auto-select so that the upper track attaches from its
bottom and the lower track from its top.
}
