% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_reads.R
\name{seq_reads}
\alias{seq_reads}
\title{IGV-style read alignment track element}
\usage{
seq_reads(
  bam,
  region,
  min_mapq = 0L,
  max_reads = 20000L,
  max_width = 100000L,
  sort_by = c("insert_length", "start"),
  link_mates = TRUE,
  show_strand = TRUE,
  aesthetics = list()
)
}
\arguments{
\item{bam}{Character. Path to an indexed BAM file.}

\item{region}{\code{GRanges} of windows to load. Every window must be
≤ \code{max_width} bp.}

\item{min_mapq}{Integer. Minimum mapping quality. Default \code{0}.}

\item{max_reads}{Integer. Maximum total reads to load (qname-aware
downsampling preserves mate pairs). Default \code{20000}.}

\item{max_width}{Integer. Maximum allowed window width in bp. Default
\code{100000}. Prevents accidental loading of entire chromosomes.}

\item{sort_by}{Character. Row-packing sort order: \code{"insert_length"}
(default — longest inserts at the top) or \code{"start"} (leftmost reads
at the top).}

\item{link_mates}{Logical. Draw a horizontal line connecting each
mated pair's outer extent. Default \code{TRUE}.}

\item{show_strand}{Logical. Draw chevron arrowheads indicating strand.
Default \code{TRUE}.}

\item{aesthetics}{Named list. Supported keys: \code{fill_plus},
\code{fill_minus}, \code{fill_unstranded}, \code{col}, \code{lwd}, \code{row_gap}, \code{tip_mm},
\code{tip_min_body_mm}, \code{link_col}, \code{link_lwd}, \code{link_lty}.}
}
\value{
A \code{SeqReadsR6} instance.
}
\description{
Renders read alignments from an indexed BAM file as chevron polygons
(pointing right for + strand reads, left for −). Mate pairs are
optionally connected by a thin horizontal line. Reads are loaded once
at construction time and cached on the element; only the requested
windows are pulled.
}
