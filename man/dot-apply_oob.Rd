% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord_ops.R
\name{.apply_oob}
\alias{.apply_oob}
\title{Apply an out-of-bounds policy to (x, y) coordinates}
\usage{
.apply_oob(
  x,
  y = NULL,
  plot_range_x,
  plot_range_y = NULL,
  mode = c("exclude", "perimeter"),
  label = ""
)
}
\arguments{
\item{x, y}{Numeric vectors of equal length. \code{y} may be \code{NULL} for
elements with only an x dimension.}

\item{plot_range_x, plot_range_y}{Length-2 numeric vectors of the
expanded plot range. \code{plot_range_y} may be \code{NULL}.}

\item{mode}{One of \code{"exclude"} (drop OOB rows; default) or
\code{"perimeter"} (clamp to the limit).}

\item{label}{Optional character used in the emitted message.}
}
\value{
A list with components:
\describe{
\item{\code{x},\code{y}}{the filtered or clamped coordinates}
\item{\code{keep}}{logical mask of which input rows survived
(always \code{TRUE} for \code{perimeter})}
\item{\code{n_oob}}{integer count of rows that were out of bounds}
}
}
\description{
Filters or clamps \verb{(x, y)} values that fall outside the (expanded) plot
range. The mode is set per-scale via \code{seq_scale_*(oob = ...)}.
}
\keyword{internal}
