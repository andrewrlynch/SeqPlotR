% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_arc.R
\name{seq_arc}
\alias{seq_arc}
\title{Bezier arch link between two genomic loci on the same track}
\usage{
seq_arc(
  data = NULL,
  mapping = NULL,
  t0 = NULL,
  t1 = NULL,
  aesthetics = aes(),
  ...
)
}
\arguments{
\item{data}{Optional \code{GRanges} or \code{data.frame}. Falls back to the parent
track's data.}

\item{mapping}{Optional \code{\link[=map]{map()}}.}

\item{t0, t1}{Track identifiers. Locked to the parent track when added
inside a \code{\link[=seq_track]{seq_track()}}.}

\item{aesthetics}{Optional \code{\link[=aes]{aes()}}: \code{color}, \code{linewidth}, \code{orientation}
(\code{"+"} / \code{"-"} / \code{"*"}), \code{curve} (\code{"length"}, \code{"equal"}, or numeric).}

\item{...}{Reserved.}
}
\value{
A \code{SeqArcR6} instance.
}
\description{
Single Bezier arch with no stems. Anchors live on the same track:
when added inside a \code{\link[=seq_track]{seq_track()}} via \verb{\%+\%}, both \code{t0} and \code{t1} are
locked to the parent track.
}
\details{
\code{data} may be a \code{GRanges} (with anchor-1 columns in \code{mcols}) or a
\code{data.frame} (BEDPE-like). \code{map()} field names are explicit:
\describe{
\item{\code{x0}, \code{x1}}{required — genomic position of each anchor.}
\item{\code{chrom0}, \code{chrom1}}{optional for GRanges (default \code{seqnames(data)});
required for \code{data.frame}.}
\item{\code{y0}, \code{y1}}{optional baselines (default \code{0}).}
\item{\code{height}}{optional arch peak height in data-scale units (default \code{1}).}
}
}
