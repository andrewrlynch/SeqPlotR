% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_segment.R
\name{seq_segment}
\alias{seq_segment}
\title{Draw line segments}
\usage{
seq_segment(data = NULL, mapping = NULL, aesthetics = aes(), ...)
}
\arguments{
\item{data}{Optional \code{GRanges}.}

\item{mapping}{Optional \code{SeqMap}. Required fields: \code{x}, \code{x_end}, \code{y},
\code{y_end}. Defaults: \code{x = start}, \code{x_end = end}, \code{y0 = 0},
\verb{y1 = resolved y}. \code{color} is auto-scaled when mapped to a non-color
column.}

\item{aesthetics}{Optional \code{SeqAes}. Supports \code{color}, \code{linewidth},
\code{linetype}, \code{alpha}.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
A \code{SeqSegmentR6} instance.
}
\description{
Primitive element drawing a straight segment from \verb{(x, y)} to
\verb{(x_end, y_end)} for each observation. When \code{map(color = col)} is used
with a non-color column, colors are auto-scaled (discrete → flexoki palette;
numeric → viridis gradient) and an auto-legend is generated.
}
\examples{
seq_segment(map(x = start, x_end = end, y = score, y_end = score))
}
