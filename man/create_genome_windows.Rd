% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{create_genome_windows}
\alias{create_genome_windows}
\title{Build genome windows from region strings}
\usage{
create_genome_windows(regions, padding = 0, genome = "hg38", add_chr = TRUE)
}
\arguments{
\item{regions}{Character vector of region strings. Each element is either
\code{"chr1:1000-2000"} / \code{"1:1,000-2,000"} (commas tolerated) or a bare
chromosome name (\code{"chr1"}, \code{"X"}, etc.). The \code{"chr"} prefix is
optional on input and controlled on output by \code{add_chr}.}

\item{padding}{Integer. Base pairs to extend on each side of every
region. Clipped so \code{start >= 1}. Default \code{0}.}

\item{genome}{Reference genome for bare-chromosome expansion. Only
\code{"hg38"} is supported.}

\item{add_chr}{Logical. If \code{TRUE} (default), output seqnames carry the
\code{"chr"} prefix.}
}
\value{
A sorted, non-overlapping \code{GRanges} of windows.
}
\description{
Parses a vector of region strings — either \code{"chr:start-end"} ranges or
bare chromosome names — into a \code{GRanges} of whole-chromosome or
sub-chromosomal windows. Bare chromosome names expand to the full length
of that chromosome in the supplied genome (via \code{\link[=default_genome_windows]{default_genome_windows()}}
when \code{genome = "hg38"}). Overlapping / adjacent regions are merged via
\code{\link[GenomicRanges:inter-range-methods]{GenomicRanges::reduce()}} before return.
}
\examples{
create_genome_windows(c("chr1", "chr2:1000000-2000000"))
create_genome_windows("chr3", padding = 5e5)
}
