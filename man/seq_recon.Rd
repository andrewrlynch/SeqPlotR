% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_recon.R
\name{seq_recon}
\alias{seq_recon}
\title{Strand-classified SV reconstruction arches}
\usage{
seq_recon(
  data = NULL,
  mapping = NULL,
  t0 = NULL,
  t1 = NULL,
  aesthetics = aes(),
  drawClasses = c("Inversion", "Dup/Del", "Translocation"),
  ...
)
}
\arguments{
\item{data}{Optional \code{GRanges} or \code{data.frame}. Falls back to the parent
track's data.}

\item{mapping}{Optional \code{\link[=map]{map()}}.}

\item{t0, t1}{Track identifiers. Locked to the parent track when added
inside a \code{\link[=seq_track]{seq_track()}}.}

\item{aesthetics}{Optional \code{\link[=aes]{aes()}}: \code{color}, \code{linewidth}, \code{orientation}
(\code{"+"} / \code{"-"} / \code{"*"}), \code{curve} (\code{"length"}, \code{"equal"}, or numeric).}

\item{drawClasses}{Character vector of tier labels to render
(default: \code{c("Inversion", "Dup/Del", "Translocation")}).}

\item{...}{Reserved.}
}
\value{
A \code{SeqReconR6} instance.
}
\description{
Inherits everything from \code{\link[=seq_arch]{seq_arch()}} and additionally classifies each
link by strand orientation and chromosome pair, drawing each class on a
fixed tier with a class-specific color. Use to summarise structural
variant calls.
}
\details{
Default colors come from \code{\link[=flexoki_palette]{flexoki_palette()}} and can be overridden via
\code{aes(h2hColor=, t2tColor=, dupColor=, delColor=, transColor=)}.

Both \code{strand0} and \code{strand1} must be specified in \code{map()} — \code{seq_recon}
errors at \code{prep()} time if either is absent.
}
