% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_highlight.R
\name{seq_highlight}
\alias{seq_highlight}
\title{Cross-track region highlight band}
\usage{
seq_highlight(
  data = NULL,
  mapping = NULL,
  t0 = NULL,
  t1 = NULL,
  aesthetics = aes(),
  ...
)
}
\arguments{
\item{data}{Optional \code{GRanges} or \code{data.frame} of one or more
highlight regions.}

\item{mapping}{Optional \code{\link[=map]{map()}}.}

\item{t0, t1}{Track identifiers (string \code{track_id} or integer index)
bracketing the inclusive run of tracks. \code{t0} is required;
\code{t1} defaults to \code{t0} (single-track highlight).}

\item{aesthetics}{Optional \code{\link[=aes]{aes()}}: \code{fill} (default \code{"grey50"}),
\code{alpha} (default \code{0.25}), \code{color} (border; default \code{NA}),
\code{linewidth} (default \code{0.5}).}

\item{...}{Reserved.}
}
\value{
A \code{SeqHighlightR6} instance.
}
\description{
Draws a continuous filled highlight band that passes through every
track from \code{t0} down to \code{t1} (inclusive), with per-track widths
determined by each track's own genomic scale. Adjacent tracks are
bridged by trapezoids in the inter-track gap, so the band fans /
compresses smoothly across tracks with different windows. Useful for
ChIP-seq style stacks where a single locus or region of interest
should be highlighted across many panels at once, or for an overview
track stacked above a zoomed detail.
}
\details{
Always used as a plot-level link — \code{t0} must be specified explicitly.
Set \code{t1 = NULL} (or omit it) to highlight a single track only.

Map vocabulary:
\describe{
\item{\code{x0}, \code{x0_end}}{required — genomic edges of the highlight
region. The same genomic region is projected onto every track in
\verb{[t0..t1]} and naturally compresses or expands per-track based on
each track's window range.}
\item{\code{chrom0}}{optional for GRanges (default \code{seqnames(data)});
required for \code{data.frame}.}
}
}
