% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_copynumber.R
\name{seq_copynumber}
\alias{seq_copynumber}
\title{Copy-number scatter plot}
\usage{
seq_copynumber(
  data,
  windows,
  cn_col = NULL,
  ratio_col = NULL,
  state_colors = NULL,
  segment_data = NULL,
  segment_col = NULL,
  show_reference_lines = TRUE,
  reference_line_col = "grey50",
  track_height = 1,
  track_id = NULL,
  legend = NULL,
  show_legend = TRUE,
  ...
)
}
\arguments{
\item{data}{A \code{GRanges} with one row per bin and mcols columns for
the CN state and the continuous ratio.}

\item{windows}{A \code{GRanges} of genomic windows defining the view.}

\item{cn_col}{Name of the mcols column giving integer CN state. Auto-
detected when \code{NULL}.}

\item{ratio_col}{Name of the mcols column giving the continuous
ratio / log-ratio. Auto-detected when \code{NULL}.}

\item{state_colors}{Named character vector keyed by CN state (as a
string). Defaults to a diverging blue -> grey -> orange palette.}

\item{segment_data}{Optional \code{GRanges} of segmentation calls. Drawn as
horizontal line segments over the scatter.}

\item{segment_col}{Column in \code{segment_data} giving the per-segment y
value (e.g. segment mean). Auto-detected from the same candidates as
\code{ratio_col} when \code{NULL}.}

\item{show_reference_lines}{Logical; draw dotted horizontal lines at
each integer CN value present in the data range. Default \code{TRUE}.}

\item{reference_line_col}{Colour for the reference lines.}

\item{track_height}{Relative track height.}

\item{track_id}{Character \code{track_id} for the generated track. Defaults
to \code{"copynumber"}.}

\item{legend}{A \code{LegendKey} or \code{SeqLegendSpec} attached to the scatter
element. \code{NULL} (default) produces no legend entry.}

\item{show_legend}{Logical. When \code{FALSE}, the scatter element contributes no
legend. Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[=seq_track]{seq_track()}}.}
}
\value{
A \code{SeqPlot} object composable via \verb{\%+\%}, \verb{\%|\%}, \verb{\%__\%}, and
\code{\link[=seq_resolve]{seq_resolve()}}.
}
\description{
Builds a \code{\link[=seq_plot]{seq_plot()}} with one track showing per-bin copy-number data:
a scatter of continuous ratio / log-ratio values, coloured by integer
CN state. Optional dotted reference lines mark integer CN values
present in the data, and an optional segmentation overlay draws a
horizontal line segment for each called segment.
}
\details{
The CN state and ratio column names are auto-detected when not
specified. For CN state the search order is
\code{c("cn", "copy_number", "CN", "state", "integer_cn")}; for ratio it
is \code{c("log2ratio", "logR", "log2R", "ratio", "log2_ratio")}. When no
match is found the first integer / numeric column is used with a
warning.
}
\examples{
library(GenomicRanges)
gr <- GRanges("chr1", IRanges(seq(1, 1e6, by = 1e4), width = 5000),
              cn        = sample(0:4, 100, replace = TRUE),
              log2ratio = rnorm(100, 0, 0.3))
win <- GRanges("chr1", IRanges(1, 1e6))
seq_copynumber(gr, windows = win)
}
