% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_chip.R
\name{seq_chip}
\alias{seq_chip}
\title{ChIP-style multi-track signal + peaks plot}
\usage{
seq_chip(
  data,
  windows,
  sample_col = NULL,
  signal_col = NULL,
  peaks = NULL,
  peak_col = NULL,
  colors = NULL,
  scale_max = NULL,
  signal_height = 1,
  peak_height = 0.25,
  show_genes = NULL,
  track_id_prefix = "",
  legend = NULL,
  show_legend = TRUE,
  ...
)
}
\arguments{
\item{data}{Either a named \code{list} of \code{GRanges} (one per sample), or a
single \code{GRanges} with a sample column (pass its name via
\code{sample_col}). Each signal \code{GRanges} must carry a numeric signal
column (auto-detected).}

\item{windows}{\code{GRanges} defining the view region.}

\item{sample_col}{Column in \code{data} giving sample identity when
\code{data} is a single \code{GRanges}. Ignored for list input.}

\item{signal_col}{Explicit signal column name; auto-detected per
sample when \code{NULL}.}

\item{peaks}{Optional peak calls, mirroring \code{data}: either a named
list of \code{GRanges} with the same names, or a single \code{GRanges} with
\code{sample_col}.}

\item{peak_col}{Optional column in peaks used for bar height; default
renders uniform-height bars.}

\item{colors}{Named character vector mapping sample name to colour.
Defaults to cycling the \code{\link[=flexoki_palette]{flexoki_palette()}}.}

\item{scale_max}{Numeric scalar or named vector capping the signal y-
axis per sample. \code{NULL} (default) autoscales.}

\item{signal_height}{Relative height of each signal track.}

\item{peak_height}{Relative height of each peak track.}

\item{show_genes}{Optional \code{GRanges} for gene annotation — adds a
final \code{\link[=seq_gene]{seq_gene()}} track beneath the sample tracks.}

\item{track_id_prefix}{Prefix prepended to all auto-generated
\code{track_id}s. Useful when composing multiple \code{seq_chip()} calls via
\code{\link[=seq_resolve]{seq_resolve()}}.}

\item{legend}{A \code{LegendKey} or \code{SeqLegendSpec} forwarded to each signal
area element. \code{NULL} (default) produces no legend entry.}

\item{show_legend}{Logical. When \code{FALSE}, signal area elements contribute no
legend. Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[=seq_track]{seq_track()}}.}
}
\value{
A \code{SeqPlot} with per-sample signal (and optionally peak) tracks.
}
\description{
For each sample, \code{seq_chip()} stacks a \code{\link[=seq_area]{seq_area()}} coverage track
on top of an optional \code{\link[=seq_bar]{seq_bar()}} peak track, colouring both from
the sample's colour. Tracks flow top-to-bottom (each one uses
\code{direction = "under"}), so the returned \code{SeqPlot} is a single
column of stacked tracks.
}
\examples{
library(GenomicRanges)
set.seed(1)
make_sig <- function() GRanges("chr1",
  IRanges(sort(sample(1:1e6, 200)), width = 500),
  score = rexp(200, rate = 0.2))
sigs <- list(S1 = make_sig(), S2 = make_sig())
seq_chip(sigs, windows = GRanges("chr1", IRanges(1, 1e6)))
}
