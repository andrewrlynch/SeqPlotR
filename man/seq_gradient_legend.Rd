% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legend.R
\name{seq_gradient_legend}
\alias{seq_gradient_legend}
\title{Create a color gradient (color-bar) legend specification}
\usage{
seq_gradient_legend(
  palette = "viridis",
  limits = c(0, 1),
  title = NULL,
  position = "inside",
  x = 0.5,
  y = 0.5,
  hjust = 0,
  orientation = NULL,
  side = NULL,
  breaks = NULL
)
}
\arguments{
\item{palette}{One of \code{"viridis"}, \code{"plasma"}, \code{"magma"}, \code{"blues"},
\code{"reds"}. Default \code{"viridis"}.}

\item{limits}{Numeric vector of length 2. The data range the gradient spans.
Default \code{c(0, 1)}.}

\item{title}{Optional character string. Legend group title.}

\item{position}{One of \code{"inside"}, \code{"track_margin"}, \code{"canvas_margin"}.}

\item{x, y}{Numeric in [0, 1]. Anchor of the bar within the target area.
Default \code{0.5}.}

\item{hjust}{Horizontal justification of the bar relative to the anchor.
Default \code{0}.}

\item{orientation}{\code{"horizontal"} or \code{"vertical"}. Bar direction.
Inferred from \code{side} when \code{NULL}.}

\item{side}{For margin positions: one of \code{"top"}, \code{"bottom"}, \code{"left"},
\code{"right"}. Defaults to \code{"top"} for margin positions.}

\item{breaks}{\code{NULL} (continuous color bar), a single positive integer
(that many evenly-spaced discrete keys), or a numeric vector of explicit
break values.}
}
\value{
A \code{GradientLegendSpec} S3 object.
}
\description{
Constructs a \code{GradientLegendSpec} describing a continuous color-scale legend
(color bar). When \code{breaks} is supplied the bar is rendered as discrete
\code{LegendKey} entries instead — useful for heatmaps that need only a handful
of labelled stops.
}
\examples{
# Continuous color bar
seq_gradient_legend(palette = "viridis", limits = c(0, 100), title = "Score")

# Five discrete keys
seq_gradient_legend(palette = "plasma", limits = c(-2, 2), breaks = 5)
}
