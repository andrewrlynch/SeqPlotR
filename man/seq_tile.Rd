% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_tile.R
\name{seq_tile}
\alias{seq_tile}
\title{Draw tiles (rectangles or rotated diamonds)}
\usage{
seq_tile(data = NULL, mapping = NULL, aesthetics = aes(), data2 = NULL, ...)
}
\arguments{
\item{data}{Optional \code{GRanges}.}

\item{mapping}{Optional \code{SeqMap}. Recognised fields: \code{x}, \code{y}, \code{fill}.}

\item{aesthetics}{Optional \code{SeqAes}. \code{rotate} toggles diamond mode;
\code{fill} sets a constant fill color.}

\item{data2}{Optional \code{GRanges} providing y-axis genomic ranges for
rotated mode. Must match \code{data} in length.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
A \code{SeqTileR6} instance.
}
\description{
Composite element drawing one filled shape per genomic range. In the
default unrotated mode, each tile is a rectangle spanning \code{start..end}
on the x-axis at the row indicated by \code{y} (default 1). With
\code{aes(rotate = TRUE)} and a \code{data2} GRanges giving per-tile y-axis
coordinates, tiles are rendered as diamonds via a linear
coordinate transform in genomic space.
}
\examples{
seq_tile(map(x = start, fill = color))
}
