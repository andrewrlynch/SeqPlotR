% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis.R
\name{.compute_axis_bands}
\alias{.compute_axis_bands}
\title{Layout the two axis slots available on each side of a track}
\usage{
.compute_axis_bands(resolved_theme, first_panel)
}
\arguments{
\item{resolved_theme}{The track's resolved theme.}

\item{first_panel}{The track's first panel (source of band bounds).}
}
\value{
A named list with one entry per side (\code{bottom}, \code{top},
\code{left}, \code{right}). Each entry is a list of up to two slots; each
slot has \code{anchor} (the npc coordinate of the panel-adjacent edge),
\code{outer} (the outer edge of the slot), and \code{band_outer} (the outer
edge of the whole track_inner margin band).
}
\description{
For each side (top / bottom / left / right) there are up to two axes
(x1 & x2 or y1 & y2) that could live there. The slot nearer the panel
is slot 1; the outer slot is slot 2. When both axes with the same
dimension share a side, x1 takes slot 1 and x2 takes slot 2; otherwise
the single axis takes slot 1 (the full band).
}
\keyword{internal}
