% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_synteny.R
\name{seq_synteny}
\alias{seq_synteny}
\title{Filled trapezoid connecting two tracks}
\usage{
seq_synteny(
  data = NULL,
  mapping = NULL,
  t0 = NULL,
  t1 = NULL,
  aesthetics = aes(),
  ...
)
}
\arguments{
\item{data}{Optional \code{GRanges} or \code{data.frame}.}

\item{mapping}{Optional \code{\link[=map]{map()}}.}

\item{t0, t1}{Track identifiers. Locked to the parent track when added
inside a \code{\link[=seq_track]{seq_track()}}.}

\item{aesthetics}{Optional \code{\link[=aes]{aes()}}: \code{fill} / \code{color} (fill color),
\code{alpha} (default \code{0.4}), \code{linewidth} (border width).}

\item{...}{Reserved.}
}
\value{
A \code{SeqSyntenyR6} instance.
}
\description{
Draws a filled quadrilateral whose bottom edge spans a region in track
\code{t0} and whose top edge spans a homologous region in track \code{t1}. Use to
highlight syntenic blocks, orthologous segments, or otherwise paired
intervals across two stacked tracks.
}
\details{
\code{data} may be a \code{GRanges} or a \code{data.frame}. Required \code{map()} fields are
\code{x0} and \code{x1}; \code{x0_end} and \code{x1_end} default to one base past \code{x0}/\code{x1}
when absent. \code{y0} and \code{y1} default to the bottom edge of t0's inner panel
and the top edge of t1's inner panel respectively — no scale conversion is
applied in that case.
}
