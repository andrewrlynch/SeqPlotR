% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis.R
\name{.expand_panels_for_combined_axis}
\alias{.expand_panels_for_combined_axis}
\title{Expand a combined virtual panel into per-original-window sub-panels}
\usage{
.expand_panels_for_combined_axis(panels)
}
\arguments{
\item{panels}{The track's panel list as returned by
\code{.build_track_panels()}.}
}
\value{
Either the input \code{panels} unchanged (when no virtual map is
present) or a list of virtual sub-panels suitable for x-axis
drawing.
}
\description{
When a track was laid out with \code{combine_windows = TRUE}, the layout
returns one panel whose xscale is virtual \code{c(1, virtual_total)}.
For axis drawing we want each original window to render its own
ticks, labels, and title at the corresponding virtual sub-range —
so produce a list of pseudo-panels covering each \code{vmap_x} entry,
with \code{xscale} set to the original genomic range and \code{inner.x0/x1}
narrowed to the virtual sub-range's npc extent.
}
\keyword{internal}
