% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{open_bigwig}
\alias{open_bigwig}
\title{Open a bigWig file connection}
\usage{
open_bigwig(path, max_fetch_bp = 50000000L)
}
\arguments{
\item{path}{Character. Path to a \code{.bw} or \code{.bigwig} file.}

\item{max_fetch_bp}{Integer. Maximum genomic span allowed per
\verb{$fetch()} call. Default \code{5e7} (50 Mb). Prevents accidental
whole-genome loads.}
}
\value{
A \code{SeqBigWig} S3 object with fields \code{path}, \code{seqnames},
\code{seqinfo}, \code{max_fetch_bp}, and a \code{fetch(region)} closure.
}
\description{
Creates a lightweight connection object that probes the bigWig header
(sequence info only — no data) and exposes a \verb{$fetch(region)} method
returning a \code{GRanges} of signal values restricted to the requested
genomic span.
}
