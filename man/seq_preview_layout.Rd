% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{seq_preview_layout}
\alias{seq_preview_layout}
\title{Preview the grid layout of a SeqPlotR plot}
\usage{
seq_preview_layout(
  plot_obj = NULL,
  layout = NULL,
  labels = TRUE,
  colors = NULL,
  margins = TRUE,
  margin_size = 0.05
)
}
\arguments{
\item{plot_obj}{A \code{seq_plot} object built with the operator chain, or \code{NULL}.
If provided, the layout is extracted from the object.}

\item{layout}{A multiline layout string (e.g. \code{"##AA\\n##AA\\nBBBC\\nBBBD"}),
or \code{NULL}. Used when \code{plot_obj} is not provided.}

\item{labels}{Logical. If \code{TRUE} (default), draw the track ID label centered
in each region.}

\item{colors}{Optional named character vector mapping track IDs to fill
colors (e.g. \code{c(A = "#FF0000", B = "#00FF00")}). Unspecified IDs receive
automatic colors.}

\item{margins}{Logical. If \code{TRUE} (default), draw a thin border rectangle
indicating the outer canvas margin.}

\item{margin_size}{Numeric in the range 0 to 0.5. Fractional canvas margin
on each side. Default \code{0.05}.}
}
\value{
A named list of npc bounding boxes (invisibly):
each element is \code{list(x0, x1, y0, y1)} keyed by track ID.
}
\description{
Renders a schematic toy representation of the intended plot layout — no data
required. Each track region is drawn as a filled, labeled rectangle.
Useful for verifying a layout string or operator chain before adding data.
}
\examples{
\dontrun{
# Preview a patchwork layout string directly
layout <- "
##AA
##AA
BBBC
BBBD
"
seq_preview_layout(layout = layout)

# Preview a layout built with operators
p <- seq_plot() |>
  (\(x) x \%|\% seq_track(track_id = "Signal",  track_width = 3))() |>
  (\(x) x \%|\% seq_track(track_id = "Ideogram", track_width = 1))() |>
  (\(x) x \%__\% seq_track(track_id = "Genes"))()
seq_preview_layout(plot_obj = p)
}

}
