% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{seq_preview_circos}
\alias{seq_preview_circos}
\title{Preview a SeqPlotR plot as a circos layout}
\usage{
seq_preview_circos(
  plot_obj = NULL,
  labels = TRUE,
  colors = NULL,
  start_angle = 90,
  end_angle = -270,
  gap_degrees = 2,
  ring_gap = 0.02,
  outer_radius = 0.45,
  inner_radius = 0.08,
  cx = 0.5,
  cy = 0.5
)
}
\arguments{
\item{plot_obj}{A \code{seq_plot} object built with the operator chain. Required.}

\item{labels}{Logical. Draw \code{track_id} labels. Default \code{TRUE}.}

\item{colors}{Optional named character vector mapping track IDs to fill
colors. Unspecified IDs receive automatic colors.}

\item{start_angle}{Numeric clock-face degrees where the first sector starts.
Default \code{90} (top). Sectors sweep clockwise (decreasing angle).}

\item{end_angle}{Numeric clock-face degrees where the last sector ends.
Default \code{-270} (full circle = \code{start_angle - 360}).}

\item{gap_degrees}{Numeric blank gap in degrees between sectors of the same
ring. Default \code{2}.}

\item{ring_gap}{Numeric npc blank gap between concentric rings. Default
\code{0.02}.}

\item{outer_radius}{Numeric npc radius of the outermost ring's outer edge.
Default \code{0.45}.}

\item{inner_radius}{Numeric npc radius of the innermost ring's inner edge.
Default \code{0.08}.}

\item{cx, cy}{Numeric npc coordinates of the circle centre. Default
\verb{(0.5, 0.5)}.}
}
\value{
Named list of polar bounding boxes (invisibly). Each entry is
\code{list(theta0, theta1, r0, r1)} in clock-face degrees and npc radii,
keyed by track ID.
}
\description{
Renders a schematic circular layout — no data required. Each track is shown
as a colored arc sector. Rows (defined via \code{direction = "under"} / \verb{\%__\%})
map to concentric rings from outer to inner; tracks within a row (defined
via \code{direction = "right"} / \verb{\%|\%}) map to arc sectors with angular width
proportional to \code{track_width}. Ring radial thickness is proportional to the
maximum \code{track_height} in that row.

Only positional layouts are supported — a plot built with a patchwork
layout string has no natural circos interpretation and is rejected.
}
\examples{
\dontrun{
p <- seq_plot() \%|\%
  seq_track(track_id = "Chr1", track_width = 3) \%|\%
  seq_track(track_id = "Chr2", track_width = 2) \%|\%
  seq_track(track_id = "Chr3", track_width = 1) \%__\%
  seq_track(track_id = "Signal") \%__\%
  seq_track(track_id = "CopyNum")
seq_preview_circos(plot_obj = p)
}

}
