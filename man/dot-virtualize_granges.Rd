% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord_ops.R
\name{.virtualize_granges}
\alias{.virtualize_granges}
\title{Virtualize a \code{GRanges} according to a virtualization map}
\usage{
.virtualize_granges(
  gr,
  vmap,
  j_start_col = NULL,
  j_end_col = NULL,
  j_chrom_col = NULL
)
}
\arguments{
\item{gr}{A \code{GRanges}.}

\item{vmap}{A virtualization map.}

\item{j_start_col, j_end_col, j_chrom_col}{Optional mcols column names
for a secondary position pair (with optional companion seqnames).
When \code{j_chrom_col} is \code{NULL}, the j seqname is taken to be the
same as the primary GRanges seqname.}
}
\value{
A virtualized \code{GRanges}, possibly shorter than \code{gr}.
}
\description{
Returns a new \code{GRanges} with seqnames replaced by the combined
sentinel, and ranges replaced by virtual coordinates. Rows whose
original (seqname, position) pair does not fall in any window are
silently dropped. Optional companion mcols giving a \emph{secondary}
position pair (e.g. \code{j_start}/\code{j_end} with an optional \code{j_chrom}
column for Hi-C) are also virtualized in place.
}
\keyword{internal}
