% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{.compute_scale_breaks}
\alias{.compute_scale_breaks}
\title{Compute breaks, labels, minor breaks, and axis_range for a scale}
\usage{
.compute_scale_breaks(scale, data_range, plot_range = NULL)
}
\arguments{
\item{scale}{A \code{SeqPositionScale}.}

\item{data_range}{Length-2 numeric vector of the unexpanded data
range. For genomic scales, this is the window range.}

\item{plot_range}{Optional pre-expanded plot range. When \code{NULL}
(default), computed from \code{data_range} via \code{.expand_limits()} using
\code{scale$expand}.}
}
\value{
A list with \code{breaks}, \code{labels}, \code{minor_breaks}, \code{axis_range},
\code{plot_range}, \code{data_range}.
}
\description{
Given a scale (continuous / genomic / discrete) and the raw data
range, returns a list with the full axis-drawing metadata: \code{breaks},
\code{labels}, \code{minor_breaks}, \code{axis_range} (where the axis line should
span, governed by \code{cap}), \code{plot_range} (the expanded visible range),
and \code{data_range}.
}
\details{
Algorithm ported from THEfunc's \code{make_axis_meta()}; uses base
\code{pretty()} instead of \code{scales::pretty_breaks()} so no new dependency
is introduced.
}
\keyword{internal}
