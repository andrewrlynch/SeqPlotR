% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{.merge_scale_with_theme}
\alias{.merge_scale_with_theme}
\title{Merge theme-shortcut scale fields into an (optional) scale object}
\usage{
.merge_scale_with_theme(scale, axis_spec)
}
\arguments{
\item{scale}{An existing scale or \code{NULL}.}

\item{axis_spec}{A per-side axis spec from \code{.build_axis_spec()}.}
}
\value{
An updated scale, or \code{NULL} when neither source supplies one.
}
\description{
The theme can express scale settings via keys like
\code{axis.x1.scale.limits} / \code{.breaks} / \code{.expand} / \code{.cap}. This helper
builds or augments the canonical scale object:
}
\details{
\itemize{
\item If \code{scale} is \code{NULL} and the theme has at least one relevant entry,
a \code{seq_scale_continuous()} is constructed from the theme values.
\item If \code{scale} is non-\code{NULL}, any \code{NULL} fields on \code{scale} are filled
from the theme. User-supplied fields always win.
}
}
\keyword{internal}
