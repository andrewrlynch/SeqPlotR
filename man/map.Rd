% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_aes.R
\name{map}
\alias{map}
\title{Capture unevaluated mapping expressions}
\usage{
map(...)
}
\arguments{
\item{...}{Named expressions (e.g. \code{x = start}, \code{y = log2(score + 1)}).}
}
\value{
A \code{SeqMap} object: a named list of unevaluated language objects.
}
\description{
Captures R expressions to be evaluated later against GRanges data. Each
expression may be a bare column name (resolved against \code{mcols(data)}), one of
the genomic specials (\code{start}, \code{end}, \code{width}, \code{mid}), or an arbitrary R
expression (e.g. \code{log2(score + 1)}, \code{(start + end) / 2}).
}
\details{
Evaluation is deferred until \code{prep()} time inside each element, where
\code{.resolve_mapping()} is called.
}
\examples{
m <- map(x = start, y = score)
}
