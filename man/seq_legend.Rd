% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legend.R
\name{seq_legend}
\alias{seq_legend}
\title{Create a legend placement specification}
\usage{
seq_legend(
  keys,
  title = NULL,
  position = "inside",
  x = 0.5,
  y = 0.5,
  hjust = 0,
  orientation = NULL,
  nrow = NULL,
  ncol = NULL,
  side = NULL
)
}
\arguments{
\item{keys}{A single \code{LegendKey}, or a named or unnamed list of \code{LegendKey}
objects. Required.}

\item{title}{Optional character string. The legend group title drawn above
or beside the key block.}

\item{position}{Character. One of \code{"inside"}, \code{"track_margin"}, or
\code{"canvas_margin"}. Controls which area the legend occupies.
Default \code{"inside"}.}

\item{x}{Numeric in [0, 1]. Horizontal position of the legend anchor
within the target area. Default \code{0.5}.}

\item{y}{Numeric in [0, 1]. Vertical position of the legend anchor
within the target area. Default \code{0.5}.}

\item{hjust}{Numeric in [0, 1]. Horizontal justification of the legend
content block relative to the anchor. \code{0} = left-aligned, \code{1} =
right-aligned. Default \code{0}.}

\item{orientation}{Character. One of \code{"horizontal"} or \code{"vertical"}.
Controls whether keys are laid out as a row or a column. When \code{NULL}
(default), orientation is inferred from \code{side}: \code{"vertical"} for
\code{side \%in\% c("left", "right")}, \code{"horizontal"} otherwise.}

\item{nrow}{Integer or \code{NULL}. Number of rows in the key grid. When both
\code{nrow} and \code{ncol} are \code{NULL}, defaults to 1 row (all keys in one row for
horizontal orientation) or 1 column (all keys in one column for vertical
orientation).}

\item{ncol}{Integer or \code{NULL}. Number of columns in the key grid.}

\item{side}{Character or \code{NULL}. For \code{position \%in\% c("track_margin", "canvas_margin")}, which margin to target. One of \code{"top"}, \code{"bottom"},
\code{"left"}, \code{"right"}. When \code{NULL}, defaults to \code{"top"} for margin
positions and is left \code{NULL} for \code{"inside"}.}
}
\value{
A \code{SeqLegendSpec} S3 object (a named list with class
\code{"SeqLegendSpec"}).
}
\description{
Constructs a \code{SeqLegendSpec} object that describes where and how a legend
group should be rendered. The spec captures placement intent only — no
rendering logic is executed here. It is consumed by the legend-rendering
layer in a later batch.
}
\examples{
k1 <- LegendKey(label = "H3K27ac", color = "firebrick")
k2 <- LegendKey(label = "H3K4me3", color = "steelblue")

# Inside the track, top-left
spec <- seq_legend(list(k1, k2), title = "Marks", x = 0.02, y = 0.95)

# In the outer track margin, bottom
spec <- seq_legend(list(k1, k2), position = "track_margin", side = "bottom")

}
