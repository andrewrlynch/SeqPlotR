% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_track.R
\name{seq_track}
\alias{seq_track}
\title{Create a new seq_track}
\usage{
seq_track(
  data = NULL,
  mapping = NULL,
  aesthetics = aes(),
  windows = NULL,
  track_id = NULL,
  direction = "right",
  track_width = 1,
  track_height = 1,
  scale_x = NULL,
  scale_y = NULL,
  scale_x2 = NULL,
  scale_y2 = NULL,
  y_windows = NULL,
  y_windows2 = NULL,
  track_outer_margin = 0.02,
  track_inner_margin = 0.02,
  window_outer_margin = 0,
  window_inner_margin = 0.02,
  window_margin = NULL,
  combine_windows = FALSE,
  combine_y_windows = FALSE,
  flip_x = FALSE,
  flip_y = FALSE,
  elements = list(),
  show_legend = TRUE,
  window_scale = NULL,
  ...
)
}
\arguments{
\item{data}{A GRanges object providing the default data for elements in this track.}

\item{mapping}{A SeqMap object from \code{\link[=map]{map()}} providing default aesthetic mappings.}

\item{aesthetics}{A SeqAes object from \code{\link[=aes]{aes()}} providing constant aesthetics.}

\item{windows}{A GRanges object defining the genomic windows for this track.
Elements cannot define their own windows — this is the only place windows are set.}

\item{track_id}{Character. Unique identifier for this track, used for patchwork
layout matching and cross-track link references.}

\item{direction}{One of \code{"right"} (append to current row) or \code{"under"} (start new row).
Ignored for the first track (always top-left) and when \code{\link[=seq_plot]{seq_plot()}} is given a
layout string.}

\item{track_width}{Relative width unit within its row. Default 1.}

\item{track_height}{Relative height unit within its column. Default 1.}

\item{scale_x}{Optional position scale for the primary x axis — a
\code{seq_scale_genomic()} (default behaviour when \code{NULL}),
\code{seq_scale_continuous(limits = ...)} for scalar data x, or
\code{seq_scale_discrete(levels = ...)} for categorical x.}

\item{scale_y}{Optional position scale for the primary y axis. Pass
\code{seq_scale_genomic(y_windows)} to flip the track so genomic
position runs along y; continuous / discrete scales are supported too.}

\item{scale_x2, scale_y2}{Optional position scales for the secondary x
and y axes. Elements routed to the secondary axis via
\code{map(axis.x = 2)} or \code{map(axis.y = 2)} are positioned against
these scales. When \code{NULL}, secondary scales are auto-inferred
from the contributing elements at \code{layoutGrid()} time.}

\item{y_windows}{Optional \code{GRanges} used as the genomic y-axis range
when flipping the track. Setting this (or a \code{SeqScaleGenomic}
\code{scale_y}) auto-enables \code{uses_genomic_y}.}

\item{y_windows2}{Optional \code{GRanges} for a secondary genomic y axis.}

\item{track_outer_margin}{Per-track outer margin in npc units. Scalar,
length-4 \code{c(bottom, left, top, right)} (base-R \code{par(mar = ...)}
order), or a named list with any of \code{top}, \code{right}, \code{bottom},
\code{left}. Default \code{0.02}. Axis titles draw here.}

\item{track_inner_margin}{Per-track inner margin. Same form as
\code{track_outer_margin}. Default \code{0.02}.}

\item{window_outer_margin}{Per-window outer margin. Same form. Default
\code{0}. Optional spacer around each window.}

\item{window_inner_margin}{Per-window inner margin. Same form. Default
\code{0.02}. Axis ticks and tick labels draw here.}

\item{window_margin}{Deprecated. Use
\verb{aes("window.gap.width" = <value>)} on the plot or track aesthetics
to control the inter-window gap. Passing a non-\code{NULL} value emits a
deprecation warning and is otherwise ignored.}

\item{combine_windows}{Logical. When \code{TRUE}, multi-region \code{windows}
are concatenated into a single virtual panel — useful for drawing
data that spans regions (e.g. inter-chromosomal Hi-C contacts) in
one continuous track. Each original window's axis labels and
title are still drawn, separated by a thin boundary marker.
Default \code{FALSE} (multi-region windows render as separate panels).}

\item{combine_y_windows}{Logical. Symmetric to \code{combine_windows}
for the genomic y-axis (only relevant for tracks with multiple
\code{y_windows}). Default \code{FALSE}.}

\item{flip_x, flip_y}{Logical. When \code{TRUE}, mirror the x or y axis
so low data values render at the high end of the panel and vice
versa. Tick labels follow the same orientation.}

\item{elements}{Optional list of \code{SeqElement} / \code{SeqLink} objects to
pre-populate this track. Elements can also be added via \code{addElement()}
or the \verb{\%+\%} operator.}

\item{show_legend}{Logical. When \code{FALSE}, this track contributes no legend
keys regardless of element \code{legend} fields. Default \code{TRUE}.}

\item{window_scale}{Numeric vector or \code{NULL}. Per-window x-axis scale
factors (e.g. \code{1e-6} for Mb, \code{1e-3} for kb, \code{1} for bp). When \code{NULL}
(default), the scale is inferred from the narrowest window. Length 1
applies the value to all windows; length equal to the number of windows
uses values positionally; any other length triggers a warning and
recycles with \code{rep_len()}.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
A \code{SeqTrackR6} instance (S3 class \code{"SeqTrack"}).
}
\description{
A track cell is partitioned into five nested zones:
\enumerate{
\item \strong{Track outer margin} — outermost band on the track cell;
reserved for axis titles derived from the track \code{mapping}.
\item \strong{Track inner margin} — separates titles from the window row.
\item \strong{Window outer margin} — per-window spacer inside the track
plot region.
\item \strong{Window inner margin} — per-window band holding axis lines,
ticks, and tick labels.
\item \strong{Plot area} — where elements render.
}
}
\details{
Axis lines sit at the boundary between the plot area and the window
inner margin. Tick labels and axis titles pick up their text from the
track's \code{mapping}: the x-axis title is the expression assigned to \code{x},
and the y-axis title is the expression assigned to \code{y}.
}
\examples{
seq_track(track_id = "A")
}
