% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawing.R
\name{drawSeqString}
\alias{drawSeqString}
\title{Draw a single cubic-Bezier string between two anchors}
\usage{
drawSeqString(
  x0,
  y0,
  x1,
  y1,
  strand1 = "*",
  strand2 = "*",
  orientation = "*",
  type = c("c", "s"),
  bulge = 0.04,
  lwd = 1.5,
  col = "red",
  alpha = 1
)
}
\arguments{
\item{x0, y0}{Canvas npc coordinates of the first anchor.}

\item{x1, y1}{Canvas npc coordinates of the second anchor.}

\item{strand1, strand2}{Strand character for each anchor (\code{"+"}, \code{"-"}, \code{"*"}).}

\item{orientation}{Optional explicit direction (\code{"+"} / \code{"-"}); when
unset, direction is inferred from \code{strand1}.}

\item{type}{\code{"c"} or \code{"s"}. Not consulted directly — retained for API
compatibility; the C vs. S shape is driven by the strand pair.}

\item{bulge}{Horizontal control-point offset in npc units. Clamped to
\verb{[0, 0.35]}.}

\item{lwd, col, alpha}{Line aesthetics.}
}
\value{
Invisible \code{NULL}.
}
\description{
Internal helper used by \code{seq_string}. Draws a smooth horizontal Bezier
curve connecting \verb{(x0, y0)} and \verb{(x1, y1)} in canvas npc coordinates. The
shape (C vs. S) is determined by the strand pair — matching strands bend
both control points the same direction, opposing strands bend them in
opposite directions.
}
\keyword{internal}
