% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_string.R
\name{seq_string}
\alias{seq_string}
\title{Cubic-Bezier string link between two genomic loci}
\usage{
seq_string(
  data = NULL,
  mapping = NULL,
  t0 = NULL,
  t1 = NULL,
  aesthetics = aes(),
  ...
)
}
\arguments{
\item{data}{Optional \code{GRanges} or \code{data.frame}.}

\item{mapping}{Optional \code{\link[=map]{map()}}.}

\item{t0, t1}{Track identifiers. Locked to the parent track when added
inside a \code{\link[=seq_track]{seq_track()}}.}

\item{aesthetics}{Optional \code{\link[=aes]{aes()}}: \code{color} (default \code{"purple"}),
\code{linewidth} (default \code{1.5}), \code{alpha} (default \code{0.8}), \code{type}
(\code{"auto"}, \code{"c"}, or \code{"s"}; default \code{"auto"}), \code{bulge}, \code{orientation}.}

\item{...}{Reserved.}
}
\value{
A \code{SeqStringR6} instance.
}
\description{
Draws a smooth Bezier curve connecting two anchors. When \code{aes(type = "auto")}
(the default), the C vs. S shape is inferred from the resolved
\code{strand0}/\code{strand1} fields — opposing strands (\verb{+/-}, \verb{-/+}) produce an
S-curve, matching strands produce a C-curve.
}
\details{
\code{data} may be a \code{GRanges} (anchor-1 columns in \code{mcols}) or a \code{data.frame}
(BEDPE-like). Required \code{map()} fields are \code{x0} and \code{x1}; \code{chrom0} and
\code{chrom1} are required for \code{data.frame} input and optional for \code{GRanges}
(default \code{seqnames(data)}). \code{y0} and \code{y1} default to \code{0}.
}
