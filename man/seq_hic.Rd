% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_hic.R
\name{seq_hic}
\alias{seq_hic}
\title{Hi-C contact matrix}
\usage{
seq_hic(
  data,
  windows,
  style = "triangle",
  max_dist = NULL,
  palette = "blues",
  na_color = "#FFFFD9",
  y_windows = NULL,
  combine_windows = FALSE,
  combine_y_windows = FALSE,
  flip_x = FALSE,
  flip_y = FALSE,
  track_height = 1,
  track_id = NULL,
  legend = NULL,
  show_legend = TRUE,
  ...
)
}
\arguments{
\item{data}{Sparse \code{GRanges} (mcols: \code{i_start}, \code{i_end}, \code{j_start},
\code{j_end}, \code{score}) or a numeric matrix / data.frame whose row/column
names encode bin positions. Each row must describe a well-formed
contact where \code{i_end - i_start} and \code{j_end - j_start} both equal
the Hi-C bin width (they define the tile's footprint on the
position and distance axes). For cross-chromosomal contacts,
include an optional \code{j_chrom} mcols column giving the j-bin's
chromosome (the GRanges's \code{seqnames} is taken to be the i-bin's
chromosome). When absent, both bins are assumed to live on the
same chromosome.}

\item{windows}{\code{GRanges} defining the genomic region(s) to display
on the x-axis. Multiple ranges produce side-by-side panels (one
per range), useful for comparing several regions.}

\item{style}{One of \code{"full"}, \code{"diagonal"}, \code{"triangle"},
\code{"rectangle"}. Default \code{"triangle"}.}

\item{max_dist}{For \code{style = "rectangle"} only: cap the distance axis
at this value (bp). Required for \code{"rectangle"}.}

\item{palette}{Colour scale palette for the tile fill. Passed to
\code{\link[=seq_scale_color_continuous]{seq_scale_color_continuous()}}.}

\item{na_color}{Colour for zero/NA contacts.}

\item{y_windows}{Optional \code{GRanges} for the genomic y-axis range in
\code{"full"} / \code{"diagonal"} styles. Defaults to \code{windows} (square
matrix). Pass a different \code{GRanges} to display an asymmetric
region pair. Ignored for rotated styles.}

\item{combine_windows}{Logical; when \code{TRUE}, multi-region \code{windows}
are concatenated into a single virtual track so cross-window data
(e.g. inter-chromosomal contacts) renders continuously in one
panel. Default \code{FALSE}.}

\item{combine_y_windows}{Symmetric to \code{combine_windows} for
multi-region \code{y_windows} in the \code{full} / \code{diagonal} styles.}

\item{flip_x, flip_y}{Logical. Mirror the x or y axis. For the
\code{triangle} style \code{flip_y = TRUE} produces a downward-pointing
triangle; for \code{diagonal} it switches to the lower diagonal; for
\code{full} it flips the matrix vertically (y) or horizontally (x).
Tick labels follow the same orientation. Default \code{FALSE}.}

\item{track_height}{Relative track height.}

\item{track_id}{\code{track_id} for the generated track. Defaults to
\code{paste0("hic_", style)}.}

\item{legend}{A \code{LegendKey} or \code{SeqLegendSpec} forwarded to the tile
element. \code{NULL} (default) produces no legend entry.}

\item{show_legend}{Logical. When \code{FALSE}, the tile element contributes no
legend. Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[=seq_track]{seq_track()}}.}
}
\value{
A \code{SeqPlot} with a single Hi-C track.
}
\description{
Builds a \code{\link[=seq_plot]{seq_plot()}} rendering a Hi-C contact matrix in one of four
styles:
\describe{
\item{\code{"full"}}{Symmetric square heatmap with genomic position on
both x- and y-axes.}
\item{\code{"diagonal"}}{Same coordinate system as \code{"full"} (kept as a
separate keyword so call sites can switch styles without changing
shape).}
\item{\code{"triangle"}}{Rotated 45 degrees; upper triangle only, y-axis
is interaction distance in base pairs.}
\item{\code{"rectangle"}}{Same rotation as \code{"triangle"}, but y-axis is
capped at \code{max_dist}, yielding a rectangle.}
}
}
\details{
To show multiple styles side-by-side, call \code{seq_hic()} multiple times
and combine via \code{\link[=seq_resolve]{seq_resolve()}} or \verb{\%|\%}.
}
\examples{
library(GenomicRanges)
set.seed(1)
n  <- 80
st <- sort(sample(seq(1, 1e6, by = 1e4), n))
gr <- GRanges("chr1", IRanges(st, width = 1e4),
              i_start = st, i_end = st + 1e4,
              j_start = st + sample(0:5e5, n, replace = TRUE),
              j_end   = st + sample(0:5e5, n, replace = TRUE) + 1e4,
              score   = rexp(n, rate = 0.5))
win <- GRanges("chr1", IRanges(1, 1e6))
seq_hic(gr, windows = win, style = "triangle")
}
