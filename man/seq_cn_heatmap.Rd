% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_cn_heatmap.R
\name{seq_cn_heatmap}
\alias{seq_cn_heatmap}
\title{Multi-sample copy-number heatmap}
\usage{
seq_cn_heatmap(
  data,
  windows,
  sample_col = NULL,
  cn_col = NULL,
  state_colors = NULL,
  sample_order = NULL,
  bins = NULL,
  track_height = 3,
  track_id = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{GRanges} (long format) or numeric matrix.}

\item{windows}{A \code{GRanges} of genomic view windows.}

\item{sample_col}{Name of the mcols column giving sample identity.
Auto-detected from \code{c("sample", "sample_id", "Sample", "id")} when
\code{NULL}.}

\item{cn_col}{Name of the mcols column giving integer CN state. Auto-
detected from the same candidates as \code{\link[=seq_copynumber]{seq_copynumber()}}.}

\item{state_colors}{Named character vector keyed by CN state string.
Defaults to the \code{\link[=seq_copynumber]{seq_copynumber()}} palette.}

\item{sample_order}{Character vector of sample names in display order
(top to bottom). When \code{NULL}, samples are sorted alphabetically.}

\item{bins}{Optional \code{GRanges} giving the genomic position of each
matrix column; required when \code{data} is a matrix.}

\item{track_height}{Relative track height.}

\item{track_id}{Character \code{track_id} for the generated track.}

\item{...}{Additional arguments forwarded to \code{\link[=seq_track]{seq_track()}}.}
}
\value{
A \code{SeqPlot} object.
}
\description{
Builds a \code{\link[=seq_plot]{seq_plot()}} with a single track arranging per-sample CN
calls as a heatmap: samples are placed on the y-axis (top to bottom
according to \code{sample_order}), genomic windows along the x-axis, and
each bin is a tile coloured by CN state.
}
\details{
Data may be supplied either as a long-format \code{GRanges} with one row
per (sample, bin) carrying \code{sample} and \code{cn} mcols columns, or as a
numeric matrix (rows = samples, cols = bins) plus a \code{bins} \code{GRanges}
giving the genomic position of each column (passed via \code{...}).
Matrix input requires the matrix argument to have sample names as
\code{rownames}.
}
\examples{
library(GenomicRanges)
gr <- GRanges("chr1", IRanges(seq(1, 1e6, by = 2e4), width = 2e4),
              sample = rep(c("S1","S2","S3"), length.out = 50),
              cn     = sample(0:4, 50, replace = TRUE))
win <- GRanges("chr1", IRanges(1, 1e6))
seq_cn_heatmap(gr, windows = win)
}
