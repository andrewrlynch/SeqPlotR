% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_bar.R
\name{seq_bar}
\alias{seq_bar}
\title{Draw bars}
\usage{
seq_bar(data = NULL, mapping = NULL, aesthetics = aes(), ...)
}
\arguments{
\item{data}{Optional \code{GRanges}. Falls back to the parent track's data.}

\item{mapping}{Optional \code{SeqMap} from \code{\link[=map]{map()}}. Recognised fields: \code{x},
\code{y} (bar height, default 1), \code{group} (stacking), \code{fill} (per-bar color).}

\item{aesthetics}{Optional \code{SeqAes} from \code{\link[=aes]{aes()}}. Supports \code{fill},
\code{color}, \code{linewidth}, \code{alpha}.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
A \code{SeqBarR6} instance.
}
\description{
Composite element drawing one filled rectangle per genomic range, spanning
\code{start..end} on the x-axis with height \code{y}. Supply a \code{group} mapping to
stack bars vertically at identical x positions.
}
\examples{
seq_bar(map(x = start, y = score))
}
