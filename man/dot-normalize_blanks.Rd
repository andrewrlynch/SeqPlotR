% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{.normalize_blanks}
\alias{.normalize_blanks}
\title{Translate bare NA values on structural axis keys to \code{visible = FALSE}}
\usage{
.normalize_blanks(flat)
}
\arguments{
\item{flat}{A flat theme map (from \code{.flatten_theme()}).}
}
\value{
The transformed flat theme.
}
\description{
Mirrors ggplot's \code{element_blank()}: when a user writes
\code{aes(axis.x.line = NA)} the intent is to hide that piece. After
flattening, scan the theme for the known structural sub-keys
(\verb{axis.<side>.line}, \code{.title}, \code{.ticks}, \code{.labels}, \code{.text},
\code{.gridline}) whose value is bare \code{NA}, and rewrite as
\verb{<key>.visible = FALSE} (dropping the raw NA leaf so it does not
confuse downstream resolvers). Also recognises a bare string on
\verb{axis.<side>.title} as a shorthand for \verb{axis.<side>.title.label}.
}
\keyword{internal}
