% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_point.R
\name{seq_point}
\alias{seq_point}
\title{Draw points}
\usage{
seq_point(data = NULL, mapping = NULL, aesthetics = aes(), ...)
}
\arguments{
\item{data}{Optional \code{GRanges}. Falls back to the parent track's data.}

\item{mapping}{Optional \code{SeqMap} from \code{\link[=map]{map()}}. Required fields: \code{x}, \code{y}.
\code{x} defaults to \code{start(data)}; \code{y} defaults to 0.5 when unmapped.
\code{color}, \code{fill}, and \code{shape} are auto-scaled when mapped to non-color
columns: discrete columns get the flexoki palette; numeric columns get
a viridis gradient legend.}

\item{aesthetics}{Optional \code{SeqAes} from \code{\link[=aes]{aes()}}. Supports \code{color}, \code{fill},
\code{size}, \code{shape}, \code{alpha}.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
A \code{SeqPointR6} instance (S3 class \code{"SeqPoint" / "SeqElement"}).
}
\description{
Primitive element drawing one glyph per observation at \verb{(x, y)}.
}
\examples{
seq_point(map(x = start, y = score))
seq_point(map(x = start, y = score, color = type))  # auto-legend
}
