% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_gene.R
\name{seq_gene}
\alias{seq_gene}
\title{Draw gene models}
\usage{
seq_gene(
  data = NULL,
  mapping = NULL,
  aesthetics = aes(),
  backbone_type = "arrow",
  show_start = FALSE,
  tss_position = NULL,
  separate_strands = FALSE,
  style_type = c("exon", "gene", "point"),
  ...
)
}
\arguments{
\item{data}{Optional \code{GRanges}.}

\item{mapping}{Optional \code{SeqMap}. Recognised fields: \code{group}
(feature-to-gene grouping; features sharing a value are one gene),
\code{strand} (default \code{"+"}), \code{label} (defaults to the group value),
\code{type} (\code{"exon"} full-height box, \code{"UTR"} 80-percent-height box,
anything else no box), and \code{color} (per-feature color).}

\item{aesthetics}{Optional \code{SeqAes}. Supports \code{color} (default
\code{"gray30"}), \code{linewidth}, \code{alpha}.}

\item{backbone_type}{Character. One of \code{"arrow"} (default), \code{"solid"}, or
\code{"dashed"}. Controls backbone line style and whether chevron arrows are
drawn. Ignored when \code{style_type} is \code{"gene"} or \code{"point"} (no backbone
exists in those modes).}

\item{show_start}{Logical. When \code{TRUE}, a TSS flag arrow is drawn above the
first exon of each gene. Default \code{FALSE}. Ignored when
\code{style_type = "point"} (the point itself marks the TSS).}

\item{tss_position}{Named list overriding per-gene TSS genomic positions.
Keys are gene IDs; values are \code{c(start, end)} genomic coordinates.
Default \code{NULL} (auto-detected from first exon by row order).}

\item{separate_strands}{Logical. When \code{TRUE}, genes are split into \code{"+"}
(top) and \code{"-"} (bottom) sub-bands, each labelled. Silently ignored when
only one strand is present. Default \code{FALSE}.}

\item{style_type}{Character. One of \code{"exon"} (default), \code{"gene"}, or
\code{"point"}. Selects the per-gene rendering style: \code{"exon"} draws the full
backbone with exon/UTR boxes; \code{"gene"} draws a single chevron-shaped
polygon spanning the gene extent (no exon detail); \code{"point"} draws a
single filled circle at the TSS. Labels are drawn in all three modes.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
A \code{SeqGeneR6} instance.
}
\description{
Composite element rendering gene models with backbone lines, exon/UTR
boxes, directional arrows, and labels. Entirely format-agnostic —
supply the relevant column names via \code{map()}.
}
\examples{
seq_gene(map(group = gene_id, strand = strand, label = gene_name))
}
