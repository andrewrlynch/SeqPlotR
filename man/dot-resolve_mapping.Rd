% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_aes.R
\name{.resolve_mapping}
\alias{.resolve_mapping}
\title{Evaluate a SeqMap against GRanges or data.frame data}
\usage{
.resolve_mapping(data, mapping, env = parent.frame())
}
\arguments{
\item{data}{A \code{GRanges} or \code{data.frame}.}

\item{mapping}{A \code{SeqMap} object (or \code{NULL}).}

\item{env}{The enclosing environment for expression evaluation.}
}
\value{
A named list of resolved vectors, one per mapping field.
}
\description{
Internal helper. Builds an evaluation environment from \code{data} and
evaluates each expression in \code{mapping} against it.
}
\details{
For a \code{GRanges}, the env is the union of:
\itemize{
\item positional specials: \code{start}, \code{end}, \code{width}, \code{mid}
\item GRanges accessors:   \code{seqnames}, \code{strand} (coerced to character)
\item the columns of \code{mcols(data)}
}

For a \code{data.frame}, the env is just \code{as.list(data)} — no specials are
injected, so column names must match the user's \code{map()} references
exactly. (Link elements rely on this: BEDPE-like inputs reference their
own column names via \code{map(x0 = start1, x1 = start2, ...)}.)

If \code{mapping} is \code{NULL} or \code{data} is \code{NULL}, returns \code{list()}.
}
\keyword{internal}
