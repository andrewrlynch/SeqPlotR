% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord_ops.R
\name{.build_virtual_map}
\alias{.build_virtual_map}
\title{Build a virtualization map for a multi-region GRanges}
\usage{
.build_virtual_map(windows, virtual_gap = 0)
}
\arguments{
\item{windows}{A \code{GRanges} of one or more windows.}

\item{virtual_gap}{Numeric (bp) gap inserted between consecutive
windows in virtual coordinates. Default \code{0}.}
}
\value{
A list with components:
\describe{
\item{\code{seqnames}}{character vector of original seqnames, length
\code{length(windows)}.}
\item{\code{genomic_start}, \code{genomic_end}}{numeric vectors of original
genomic ranges.}
\item{\code{virtual_start}, \code{virtual_end}}{numeric vectors of the
corresponding virtual ranges.}
\item{\code{virtual_total}}{the total virtual extent (right edge of the
last window).}
\item{\code{virtual_gap}}{the gap size used (echoed back).}
\item{\code{combined_window}}{a single-range \code{GRanges} covering the
full virtual extent, used in place of \code{windows} for layout.}
}
}
\description{
Concatenates the windows into a single virtual coordinate system in
insertion order. Each window contributes \code{width(window)} to the
virtual axis, with optional gaps (\code{virtual_gap}) inserted between
windows so a separator can be drawn at the boundaries.
}
\keyword{internal}
