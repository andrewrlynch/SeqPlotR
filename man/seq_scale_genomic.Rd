% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{seq_scale_genomic}
\alias{seq_scale_genomic}
\title{Genomic position scale}
\usage{
seq_scale_genomic(
  windows,
  scale_factor = NULL,
  breaks = NULL,
  minor_breaks = NULL,
  expand = c(0, 0),
  cap = c("capped", "full", "exact", "ticks"),
  labels = NULL
)
}
\arguments{
\item{windows}{A \code{GRanges} object defining the genomic windows.}

\item{scale_factor}{Numeric vector of per-window scale factors controlling
the unit label (1e-6 = Mb, 1e-3 = kb, 1 = bp). If \code{NULL}, reads from
\code{mcols(windows)$scale} or defaults to \code{1e-6}.}

\item{breaks}{Optional numeric vector of explicit break positions. When
supplied, these are used instead of \code{pretty()}-generated breaks.}

\item{minor_breaks}{Optional scalar (number of sub-divisions between
major breaks) or numeric vector of explicit minor-break positions.}

\item{expand}{Length-2 \code{c(mul, add)} specifying multiplicative and
additive padding around the data range. Default \code{c(0, 0)} for
genomic (windows already set the visible range).}

\item{cap}{One of \code{"capped"} (axis line spans the break range),
\code{"full"} (spans the expanded plot range), \code{"exact"} (spans the
unexpanded data range), or \code{"ticks"} (no axis line, only ticks).}

\item{labels}{Optional character vector of tick labels (same length as
\code{breaks}). If \code{NULL}, breaks are formatted as decimal numbers.}
}
\value{
A \code{SeqScaleGenomic} / \code{SeqPositionScale} object.
}
\description{
Creates a position scale based on genomic coordinates from a \code{GRanges} object.
Used for axes that represent physical genome positions (bp, kb, Mb).
}
