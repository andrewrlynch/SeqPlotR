% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_resolve.R
\name{seq_resolve}
\alias{seq_resolve}
\title{Compose wrapper-built plots into a parent plot}
\usage{
seq_resolve(parent, ..., direction = "under")
}
\arguments{
\item{parent}{A \code{seq_plot} object to add tracks into.}

\item{...}{One or more \code{seq_plot} objects produced by wrapper
functions.}

\item{direction}{\code{"under"} (default) or \code{"right"}. See Details.}
}
\value{
The modified \code{parent}, invisibly (R6 reference semantics).
}
\description{
Each wrapper (e.g. \code{\link[=seq_copynumber]{seq_copynumber()}}, \code{\link[=seq_hic]{seq_hic()}}, \code{\link[=seq_chip]{seq_chip()}})
returns a \code{seq_plot}. \code{seq_resolve()} unpacks those child plots into
a parent \code{seq_plot}, letting you combine heterogeneous track stacks
in a single figure.
}
\details{
Tracks from each child are appended to \code{parent} respecting their
original row grouping. The \code{direction} argument controls where the
\strong{first} row of each child lands relative to the current bottom of
\code{parent}:
\itemize{
\item \code{"under"} — start a new row below whatever is already in
the parent (the common case).
\item \code{"right"} — append to the current row (put the child beside
what is already there).
}

Subsequent rows within a child are always placed \code{"under"} (i.e. the
child's internal row structure is preserved). Multiple children
supplied in one call are stacked in order, each with \code{direction}
applied between children.

Plot-level links and annotations carried by the children are
transferred to \code{parent} in document order.

Duplicate \code{track_id}s across children cause an error — pass unique
\code{track_id} values to each wrapper call to resolve the conflict.
}
\examples{
\dontrun{
win <- GRanges("chr1", IRanges(1, 1e6))
cn  <- seq_copynumber(cn_gr, windows = win, track_id = "CN")
hic <- seq_hic(hic_gr, windows = win, style = "triangle",
               track_id = "HiC")
seq_resolve(seq_plot(), cn, hic)$plot()
}
}
